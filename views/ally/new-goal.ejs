<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <% include ../partials/head.ejs %>
    </head>
    <body>
        <div class="container top">
            <img class="img-fluid" src="/images/green_wave.jpg" alt=""/>
            <form class="logout-small" action="/sessions?_method=DELETE" method="POST">
                <input class="btn btn-light btn-sm" type="submit" value="Log Out">
            </form>
            <div class="container header">
                <h1>Set A New Goal</h1>
                <h4>Tackle goals at your own pace. Start small, choose goals that are achievable and undaunting.</h4>
                <a href="/ally"><button class="btn btn-light btn-lg back-to-main" type="button" name="back">Back To Main</button></a>
            </div>
        </div>
        <div class="container col d-flex justify-content-center">
            <form class="form-inline" action="/ally/new-goal" method="POST">
                <input class="form-control form-control-lg" type="text" name="description" placeholder="your goal">
                <button class="new-goal-button btn btn-info" type="submit" name="">Let's Go</button>
            </form>
        </div>
        <div class="container completed-goals">
            <h3>Your Completed Goals</h3>
            <ul>
                <% for (let i = 0; i < goals.length; i++) { %>
                    <% if (goals[i].completed === true) { %>
                        <li>
                            Description: <%=goals[i].description%>
                        </li>
                    <% } %>
                <% } %>
            </ul>
        </div>
    </body>
</html>
