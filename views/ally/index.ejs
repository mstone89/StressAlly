<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <div class="container">
            <!-- ADD IMAGE HERE -->
            <div class="goal">
                <h1>I want to:</h1>
                <!-- <% if (goals.length) { %>
                    <% for (let i = 0; i < goals.length; i++) { %>
                        <% if (goals[i].completed === false) { %>
                            <h2><%=goals[i].description%></h2>
                            <form action="/ally/<%=goals[i].id%>/complete-goal?_method=PUT" method="POST">
                                <button type="submit" name="completed">Goal Completed</button>
                            </form>
                        <% } else { %>
                            <h2>set a new goal</h2>
                            <a href="/ally/new-goal"><button type="button" name="new-goal">New Goal</button></a>
                        <% } %>
                    <% } %>
                <% } else { %>
                    <h2>set a new goal</h2>
                    <a href="/ally/new-goal"><button type="button" name="new-goal">New Goal</button></a>
                <% } %> -->
            </div>
            <div class="activity">
                <h1>My next activity:</h1>
                <!-- EJS CONDITIONALS -->
                <form class="">
                    <button type="submit" name="completed">Activity Completed</button>
                </form>
                <form class="">
                    <button type="submit" name="new-activity">New Activity</button>
                </form>
            </div>
        </div>
        <div class="container">
            <a href="/ally/new-entry"><button type="button" name="new-entry">New Entry</button></a>
            <a href="/ally/entries"><button type="button" name="entries">Your Entries</button></a>
            <a href="#"><button type="button" name="goals">Completed Goals</button></a>
            <a href="#"><button type="button" name="resources">Your Resources</button></a>
        </div>
    </body>
</html>
