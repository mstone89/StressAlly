<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <% include ../partials/head.ejs %>
    </head>
    <body>
        <div class="container top">
            <img class="img-fluid" src="/images/entry_image.jpg" alt=""/>
            <form class="logout-small" action="/sessions?_method=DELETE" method="POST">
                <input class="btn btn-light btn-sm" type="submit" value="Log Out">
            </form>
            <div class="container header">
                <h1>Edit Entry</h1>
                <a href="/ally"><button class="btn btn-light btn-lg back-to-main" type="button" name="back">Back To Main</button></a>
            </div>
        </div>
        <div class="container fieldset">
            <form class="new-entry" action="/ally/entry/<%=entry.id%>?_method=PUT" method="POST">
                <fieldset class="mx-auto">
                    <legend>How Are You Doing Today?</legend>
                    <div class="d-flex flex-column">
                        <div>
                            <label>Anxiety Rating:</label>
                            <input type="number" name="rating" value="<%=entry.rating%>" min="0" max="5"/>
                        </div>
                        <div>
                            <label>Foods You've Eaten Today:</label>
                            <input type="text" name="food" value="<%=entry.food%>"/>
                        </div>
                        <div>
                            <label>Hours Of Sleep:</label>
                            <input type="number" name="sleep" value="<%=entry.sleep%>" min="0"/>
                        </div>
                        <div>
                            <label>Meditated?</label>
                            <input type="checkbox" name="meditate"
                            <% if (entry.meditate === true) { %>
                                checked
                            <% } %>
                            />
                        </div>
                        <div>
                            <label>Exercised?</label>
                            <input type="checkbox" name="exercise"
                            <% if (entry.exercise === true) { %>
                                checked
                            <% } %>
                            />
                        </div>
                        <div>
                            <label>Comments:</label>
                            <input name="comments" value="<%=entry.comments%>"/>
                        </div>
                    </div>
                </fieldset>
                <input class="btn btn-info" type="submit" value="Submit Changes">
            </form>
        </div>
    </body>
</html>
