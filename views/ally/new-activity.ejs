<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <% include ../partials/head.ejs %>
    </head>
    <body>
        <div class="container top">
            <img class="img-fluid" src="/images/green_wave.jpg" alt=""/>
            <form class="logout-small" action="/sessions?_method=DELETE" method="POST">
                <input class="btn btn-light btn-sm" type="submit" value="Log Out">
            </form>
            <div class="container header">
                <h1>Your Activities</h1>
                <h4>Keep active: exercise, meditation, walking -- any small activity can reduce stress and anxiety.</h4>
                <a href="/ally"><button class="btn btn-light btn-lg back-to-main" type="button" name="back">Back To Main</button></a>
            </div>
        </div>
        <div class="container col d-flex justify-content-center">
            <h3>Add a new activity</h3>
        </div>
        <div class="container col d-flex justify-content-center">
            <form class="form-inline" action="/ally/new-activity" method="POST">
                <input class="form-control form-control-lg" type="text" name="name" placeholder="activity name">
                <input class="btn btn-info" type="submit" name="" value="create">
            </form>
        </div>
        <div class="container">
            <h3>Choose an existing activity</h3>
            <% for (let i = 0; i < activities.length; i++) { %>
                <form action="/ally/activity/<%=activities[i].id%>?_method=PUT" method="POST">
                    <button class="activity-btn btn btn-info" type="submit" name="active"><%=activities[i].name%></button>
                </form>
            <% } %>
        </div>
    </body>
</html>
