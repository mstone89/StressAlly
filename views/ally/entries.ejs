<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <div class="container">
            <!-- ADD IMAGE HERE -->
            <h1>Your Entries</h1>
        </div>
        <div class="container">
            <ul>
                <% for (let i = 0; i < entries.length; i++) { %>
                    <li>
                        Date: <%=entries[i].date%>
                        Rating: <%=entries[i].rating%>
                        <a href="/ally/<%=entries[i].id%>/edit"><button>Edit Entry</button></a>
                        <form action="/ally/<%=entries[i].id%>?_method=DELETE" method="POST">
                            <input type="submit" value="Delete Entry"/>
                        </form>
                    </li>
                <% } %>
            </ul>
        </div>
        <a href="/ally"><button type="button" name="back">Back To Main</button></a>
    </body>
</html>
